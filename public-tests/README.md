# Check público de Semana 2

Desde la raíz del proyecto: `bash public-tests/check.sh`.

Comprueba que existan los artefactos obligatorios de shell/manifest y ejecuta una búsqueda básica para evitar secretos o tokens. No sustituye `make verify` ni la revisión del commit fijado.
